#include "main.h"
/**
 * _strcmp -  compares two strings.
 * @s1: first str
 * @s2: second str
 * Return: comparison result
*/
int _strcmp(char *s1, char *s2)
{
        int i = 0;

	while (*s1 != '\0' && *s2 != '\0')
	{
		return(s1[i] - s2[2]);

	}
	return(0);
}
